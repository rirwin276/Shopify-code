/*
 * Shopify theme sync marker — BC3501Y Youth Long Sleeve Tee
 *
 * The active Pro Builder catalog asset already fetches newly installed
 * Product Lab builders from Printful_Automation's /api/pro-builder-catalog
 * endpoint. BC3501Y is now installed and served there. This asset intentionally
 * contains no runtime code; committing it advances the connected Shopify theme
 * branch so the live theme receives the current dynamic-catalog implementation
 * and can render the new youth long-sleeve card.
 *
 * Safe to remove after the Shopify theme reports this commit as synced.
 */
