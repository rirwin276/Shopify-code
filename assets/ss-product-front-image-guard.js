/*
  Stella & Sage — Product Front Image Guard

  TEMP DISABLED:
  The previous carousel correction script could trigger Shopify's product
  zoom/lightbox on desktop. Leaving this asset as a safe no-op because it is
  still loaded by theme.liquid on product pages.

  Next clean fix should happen in product media ordering / variant media logic,
  not by clicking or forcing gallery controls from JavaScript.
*/
(function () {
  'use strict';
  return;
})();
