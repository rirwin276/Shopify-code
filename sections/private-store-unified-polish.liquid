{{ 'storefront-unified-polish.css' | asset_url | stylesheet_tag }}
{{ 'storefront-tab-strip-fix.css' | asset_url | stylesheet_tag }}
{{ 'storefront-final-tune.css' | asset_url | stylesheet_tag }}

{% comment %}
  Keep this section intentionally thin.

  The external stylesheets above are the authoritative private-store appearance
  layers, in cascade order. Do not add another inline preset stylesheet below
  them: doing so previously overrode storefront-final-tune.css and made later
  fixes appear to have no effect in both the real draft storefront and the
  script-stripped Admin Powers snapshot.
{% endcomment %}

<script src="{{ 'storefront-live-structure.js' | asset_url }}" defer></script>
<script src="{{ 'storefront-tab-runtime-fix.js' | asset_url }}" defer></script>

{% schema %}
{
  "name": "Storefront unified polish",
  "enabled_on": { "templates": ["collection"] },
  "settings": []
}
{% endschema %}
