{%- assign activity_handle = handle | default: '' | downcase | strip -%}
{%- if activity_handle != blank -%}
  <span hidden data-storefront-activity-tracker="{{ activity_handle | escape }}"></span>
{%- endif -%}
